# Legwan — electron-builder configuration
# Produces a Windows NSIS installer (.exe) and a portable ZIP.

appId: cm.legwan.manager
productName: Legwan
copyright: "© 2026 Legwan. Tous droits réservés."

# ── Code signing — disabled (no certificate yet) ───────────────────────────────
# Also set CSC_IDENTITY_AUTO_DISCOVERY=false in the build script.
forceCodeSigning: false

# ── Files to include in the app package ────────────────────────────────────────
directories:
  output: release
  buildResources: build

files:
  - dist/**/*
  - electron/**/*
  - package.json
  - "!node_modules/**/*"
  - "!src/**/*"
  - "!tests/**/*"
  - "!*.test.*"

# ── Windows configuration ──────────────────────────────────────────────────────
win:
  target:
    - target: nsis
      arch:
        - x64
        - ia32
    - target: zip
      arch:
        - x64
  icon: build/icon.ico
  requestedExecutionLevel: asInvoker
  # Disable rcedit resource editing — avoids downloading winCodeSign which
  # fails on Windows without Developer Mode (macOS symlinks in the archive).
  # The icon is still embedded in the NSIS installer itself.
  signAndEditExecutable: false

# ── NSIS installer wizard ──────────────────────────────────────────────────────
nsis:
  oneClick: false
  allowElevation: true
  allowToChangeInstallationDirectory: true
  perMachine: false

  # French interface (fr-FR = BCP 47, language code 1036 = fr)
  installerLanguages:
    - fr-FR
  language: "1036"

  # Shortcuts
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Legwan

  # License shown INSIDE the NSIS wizard before installation
  license: build/LICENSE.rtf

  # Installer branding
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
  runAfterFinish: true

# ── Auto-update (GitHub Releases) ─────────────────────────────────────────────
# Uncomment once the first release is published on GitHub
# publish:
#   provider: github
#   owner: Breniell
#   repo: shopnova-manager
#   releaseType: release
